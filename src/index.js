import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

// Hou comment: Consider storing your bee- and hive- images inside an /images folder.
